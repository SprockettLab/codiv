% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo_host_analysis.R
\name{loo_host_analysis}
\alias{loo_host_analysis}
\title{Leave-One-Host-Out Analysis}
\usage{
loo_host_analysis(
  Host_tree,
  Symbiont_tree,
  Host_to_Symbiont_df,
  hosts_to_test = NULL,
  alpha = 0.05,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{Host_tree}{A phylo object; binary phylogenetic tree of hosts}

\item{Symbiont_tree}{A phylo object; binary phylogenetic tree of symbionts}

\item{Host_to_Symbiont_df}{A data frame with columns "Host" and "Symbiont"
linking symbionts to hosts}

\item{hosts_to_test}{Character vector of host names to test. If NULL (default),
tests all hosts. Use a subset to reduce computation time.}

\item{alpha}{Significance threshold for counting significant nodes; default 0.05}

\item{verbose}{If TRUE (default), report progress across hosts}

\item{...}{Additional arguments passed to \code{\link[=codiv]{codiv()}} (e.g. min_hosts,
min_symbiont_tips, span_fraction, permutations, methods, cores, seed)}
}
\value{
A list with elements:
\itemize{
\item \code{full_results}: the \code{\link[=codiv]{codiv()}} result on the complete data
\item \code{loo_results}: named list of the \code{\link[=codiv]{codiv()}} result with each host removed
\item \code{host_importance}: one row per tested host (ordered by \code{impact_score}),
with the number of nodes scanned, per-method significant-node counts and
their change from the full analysis (\verb{<method>_delta_n_sig}), and an
\code{impact_score} (mean absolute change in significant-node count across
methods when the host is removed)
}
}
\description{
Reruns \code{\link[=codiv]{codiv()}} with each host removed in turn and compares each run to the
full analysis, showing how much each host contributes to the
codiversification signal.
}
\details{
Each run uses its own isolated checkpoint file so removing a host genuinely
re-computes the affected nodes. Tree-distance metrics are disabled for speed.
Runtime is roughly \verb{baseline_time x length(hosts_to_test)}, so consider
\code{hosts_to_test}, fewer permutations, or a single method for a first pass, and
\code{cores} to parallelize each run.
}
\examples{
\donttest{
sim <- simulate_codiv_data(n_hosts = 8, n_clades = 2, seed = 1)
loo <- loo_host_analysis(sim$host_tree, sim$symbiont_tree, sim$links,
                         permutations = 99, methods = "hommola",
                         verbose = FALSE)
loo$host_importance
}

}
