% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_codiv_trees.R
\name{plot_codiv_trees}
\alias{plot_codiv_trees}
\title{Visualize Host-Symbiont Codiversification}
\usage{
plot_codiv_trees(
  Host_tree,
  Symbiont_tree,
  Host_to_Symbiont_df,
  codiv_results = NULL,
  color_by = c("host", "none", "significance"),
  significance_threshold = 0.05,
  line_width = 0.5,
  line_alpha = 0.6,
  title = NULL,
  use_procrustes = TRUE,
  show_host_labels = TRUE,
  show_symbiont_labels = FALSE,
  label_size = 3.2,
  use_branch_lengths = FALSE
)
}
\arguments{
\item{Host_tree}{A phylo object; host phylogenetic tree}

\item{Symbiont_tree}{A phylo object; symbiont phylogenetic tree}

\item{Host_to_Symbiont_df}{A data frame with columns "Host" and "Symbiont"
linking symbionts to hosts}

\item{codiv_results}{Optional \code{\link[=codiv]{codiv()}} result; required for
\code{color_by = "significance"}}

\item{color_by}{How to colour the association lines: "host" (default, by
host), "none", or "significance" (symbionts in significant nodes)}

\item{significance_threshold}{p-value threshold for \code{color_by = "significance"}; default 0.05}

\item{line_width}{Width of the association lines; default 0.5}

\item{line_alpha}{Transparency of the association lines (0-1); default 0.6}

\item{title}{Plot title; default NULL}

\item{use_procrustes}{If TRUE (default), rotate the symbiont tree to align it
with the host tree and reduce line crossings}

\item{show_host_labels}{If TRUE (default), label host tips}

\item{show_symbiont_labels}{If TRUE, label symbiont tips; default FALSE
(symbiont trees are often large)}

\item{label_size}{Tip label text size; default 3.2}

\item{use_branch_lengths}{If FALSE (default), use a cladogram layout (tips
aligned, nodes spread by topology) for an uncluttered view; if TRUE, scale
horizontal position by branch length}
}
\value{
A ggplot object, customizable with further ggplot2 layers and
saveable with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
Draws a tanglegram: the host tree on the left, the symbiont tree mirrored on
the right, and a line for each host-symbiont association. Optionally rotates
the symbiont tree to align it with the host tree and colours the association
lines.
}
\details{
Requires the ggplot2 package. The tree layout is computed with ape and the
alignment uses \code{\link[ape:rotate]{ape::rotateConstr()}}. By default a cladogram layout is used
(tips aligned, nodes spread by topology); set \code{use_branch_lengths = TRUE} to
position nodes by branch length instead.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  sim <- simulate_codiv_data(n_hosts = 8, n_clades = 2, seed = 1)
  plot_codiv_trees(sim$host_tree, sim$symbiont_tree, sim$links,
                   color_by = "host")
}
}

}
