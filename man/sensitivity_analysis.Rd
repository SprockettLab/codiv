% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_analysis.R
\name{sensitivity_analysis}
\alias{sensitivity_analysis}
\title{Parameter Sensitivity Analysis}
\usage{
sensitivity_analysis(
  Host_tree,
  Symbiont_tree,
  Host_to_Symbiont_df,
  span_fraction_range = c(0.05, 0.1, 0.2, 0.3),
  min_symbiont_tips_range = c(5, 7, 10, 15),
  permutations = 99,
  min_hosts = 3,
  methods = c("hommola", "paco", "parafit"),
  alpha = 0.05,
  cores = 1,
  seed = 8675309,
  verbose = TRUE
)
}
\arguments{
\item{Host_tree}{A phylo object; binary phylogenetic tree of hosts}

\item{Symbiont_tree}{A phylo object; binary phylogenetic tree of symbionts}

\item{Host_to_Symbiont_df}{A data frame with columns "Host" and "Symbiont"}

\item{span_fraction_range}{Numeric vector of \code{span_fraction} values to test;
default c(0.05, 0.1, 0.2, 0.3)}

\item{min_symbiont_tips_range}{Integer vector of \code{min_symbiont_tips} values;
default c(5, 7, 10, 15)}

\item{permutations}{Integer; number of permutations, applied to every run}

\item{min_hosts}{Integer; minimum hosts, applied to every run (>= 3)}

\item{methods}{Character vector of methods to use}

\item{alpha}{Significance threshold for counting significant nodes; default 0.05}

\item{cores}{Number of cores passed to \code{\link[=codiv]{codiv()}}}

\item{seed}{Random seed for reproducibility}

\item{verbose}{If TRUE (default), report progress across the grid}
}
\value{
A list with elements:
\itemize{
\item \code{sensitivity_data}: one row per parameter combination, with the number of
nodes scanned and, per method, the number of significant nodes and the
mean of the primary statistic
\item \code{results_by_params}: named list of the full \code{\link[=codiv]{codiv()}} result for each
parameter combination
\item \code{heatmap_data}: long-format data (one row per combination and method) with
\code{n_sig}, \code{n_nodes}, and \code{prop_sig}, ready for plotting
}
}
\description{
Reruns \code{\link[=codiv]{codiv()}} across a grid of \code{span_fraction} and \code{min_symbiont_tips}
values and summarizes how the number of scanned and significant nodes changes,
so you can see whether findings are stable or threshold-dependent.
}
\details{
Runtime is roughly \verb{baseline_time x length(span_fraction_range) x length(min_symbiont_tips_range)}. Tree-distance metrics are turned off for
speed, since sensitivity is judged from node counts and significance.
}
\examples{
\donttest{
sim <- simulate_codiv_data(n_hosts = 10, n_clades = 3, seed = 1)
sens <- sensitivity_analysis(sim$host_tree, sim$symbiont_tree, sim$links,
                             span_fraction_range = c(0.3, 0.6),
                             min_symbiont_tips_range = c(7, 10),
                             permutations = 99, methods = "hommola",
                             verbose = FALSE)
sens$sensitivity_data
}

}
