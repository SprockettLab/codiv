% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_codiv_data.R
\name{simulate_codiv_data}
\alias{simulate_codiv_data}
\title{Simulate host and symbiont trees with known co-diversification}
\usage{
simulate_codiv_data(
  n_hosts = 12,
  clades = NULL,
  n_clades = NULL,
  prop_codiversifying = 0.5,
  congruence_range = c(0.5, 1),
  n_per_host_range = c(1, 4),
  bl_noise = 0.1,
  host_tree = NULL,
  seed = NULL
)
}
\arguments{
\item{n_hosts}{Number of host tips (ignored if \code{host_tree} is supplied)}

\item{clades}{Optional list of clade specifications, each a list with
\code{codiversifying} (logical) and optionally \code{congruence} and \code{n_per_host}.
If NULL, \code{n_clades} random clades are generated.}

\item{n_clades}{Number of random clades to generate when \code{clades} is NULL}

\item{prop_codiversifying}{Probability a random clade is co-diversifying}

\item{congruence_range}{Range to draw random co-diversifying \code{congruence} from}

\item{n_per_host_range}{Integer range to draw random \code{n_per_host} from}

\item{bl_noise}{Multiplicative branch-length jitter (0 = none)}

\item{host_tree}{Optional host tree (phylo); if NULL a random coalescent tree
of \code{n_hosts} tips is used}

\item{seed}{Optional random seed}
}
\value{
A list with:
\itemize{
\item \code{host_tree}: the host tree (phylo)
\item \code{symbiont_tree}: the combined symbiont tree (phylo)
\item \code{links}: data frame of Host-Symbiont associations
\item \code{truth}: per-symbiont ground truth (clade, codiversifying, congruence,
n_per_host)
}
}
\description{
Builds a host tree and a symbiont tree assembled from several clades, each
with known ground truth, all mapping to the same set of hosts. Use it for a
fixed sanity-check or to generate many random datasets to study how tree
features affect the scan.
}
\details{
Each clade is one monophyletic block of the symbiont tree, controlled by:
\itemize{
\item \code{codiversifying}: whether its topology tracks the host tree
\item \code{congruence}: for co-diversifying clades, how closely it tracks the host
(1 = identical branching order; lower = more scrambled)
\item \code{n_per_host}: symbionts sampled per host (1 = no within-host
pseudoreplication; > 1 introduces it)
}
}
\examples{
# fixed four-clade sanity check
sim <- simulate_codiv_data(
  n_hosts = 10,
  clades = list(
    list(codiversifying = TRUE,  congruence = 1.0, n_per_host = 1),
    list(codiversifying = TRUE,  congruence = 0.6, n_per_host = 4),
    list(codiversifying = FALSE, n_per_host = 1),
    list(codiversifying = FALSE, n_per_host = 4)
  ),
  seed = 1
)
sim$host_tree
head(sim$links)

# many random datasets (vary seed) to study tree-feature effects
rand <- simulate_codiv_data(n_hosts = 15, n_clades = 6, seed = 42)

}
