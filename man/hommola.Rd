% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hommola.R
\name{hommola}
\alias{hommola}
\title{Calculate Hommola correlation coefficient}
\usage{
hommola(i_host_subtree_dist, i_symbiont_subtree_dist, i_host_to_symbiont_df)
}
\arguments{
\item{i_host_subtree_dist}{A distance matrix (e.g., from \code{dist()} or
\code{adephylo::distTips()}) for the host subtree tips}

\item{i_symbiont_subtree_dist}{A distance matrix for the symbiont subtree tips}

\item{i_host_to_symbiont_df}{A data frame with "Host" and "Symbiont" columns
linking each symbiont to its host}
}
\value{
A numeric correlation coefficient (Pearson's r) ranging from -1 to 1.
Higher positive values indicate symbiont topology more closely matches
host topology. Returns 0 if all host distances are identical (degenerate case).
}
\description{
Computes Pearson correlation between pairwise phylogenetic distances in
host and symbiont trees. Tests the null hypothesis of no association
between host and symbiont topology (Hommola et al. 2011).
}
\examples{
\donttest{
library(adephylo)
h_tree <- ape::rtree(5)
s_tree <- ape::rtree(15)
hs_df <- data.frame(
  Host = rep(h_tree$tip.label, 3),
  Symbiont = s_tree$tip.label
)
h_dist <- distTips(h_tree, method = "patristic")
s_dist <- distTips(s_tree, method = "patristic")
r <- hommola(h_dist, s_dist, hs_df)
}

}
