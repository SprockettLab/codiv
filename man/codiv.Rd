% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codiv.R
\name{codiv}
\alias{codiv}
\title{Perform codiversification scan between host and symbiont trees}
\usage{
codiv(
  Host_tree,
  Symbiont_tree,
  Host_to_Symbiont_df,
  min_hosts = 3,
  min_symbiont_tips = 7,
  max_symbiont_tips = NULL,
  span_fraction = 0.1,
  uncollapsed_hommola = FALSE,
  permutations = 99,
  seed = 8675309,
  verbose = TRUE,
  Save_fp = NULL,
  methods = c("hommola", "paco", "parafit"),
  focus_hosts = NULL,
  subtree_features = TRUE,
  cores = NULL,
  batch_size = NULL,
  n_batches = NULL,
  continue = TRUE
)
}
\arguments{
\item{Host_tree}{A phylo object; binary phylogenetic tree of hosts}

\item{Symbiont_tree}{A phylo object; binary phylogenetic tree of symbionts}

\item{Host_to_Symbiont_df}{A data frame with columns "Host" and "Symbiont"
linking each symbiont tip to its source host}

\item{min_hosts}{Minimum number of hosts required at a node for inclusion
in the scan; default 3}

\item{min_symbiont_tips}{Minimum number of symbiont tips in a subtree for
inclusion; default 7 (higher values recommended for statistical power)}

\item{max_symbiont_tips}{Maximum number of symbiont tips in a subtree. If
NULL (default), \verb{max(500, 2\% of the tree's tips)}. This is not the same
control as \code{span_fraction}: that limits how deep a clade is, this limits
how many genomes it holds, and the two are only loosely related. A
densely sampled recent radiation is enormous but shallow, so it passes
any reasonable span cutoff. Very large clades are also where the
permutation test reaches significance at trivial effect sizes simply
because n is large.}

\item{span_fraction}{Filters nodes to only those with phylogenetic span
<= this fraction of total tree span (0-1); default 0.1 focuses on distal nodes}

\item{uncollapsed_hommola}{If TRUE, also run the Hommola test on the
uncollapsed subtree and report \code{Uncollapsed_Hommola_r}/\code{Uncollapsed_Hommola_pvalue}. Default
FALSE. The uncollapsed test counts every MAG from a host that formed a
monophyletic group as an independent observation, so a deeply sampled
host is pseudoreplicated; PACo and ParaFit already use the collapsed
tree. It is also by far the most expensive statistic on large clades.}

\item{permutations}{Number of randomizations for significance testing;
default 99; higher values (999+) recommended for publication}

\item{seed}{Random seed for reproducible permutations; default 8675309}

\item{verbose}{If TRUE (default), prints progress and filtering statistics}

\item{Save_fp}{File path where results are saved after each node (enables
resumable runs via \code{continue}); if NULL (default), a unique temporary file
is used, so separate calls never share a checkpoint. Supply an explicit
path to enable resuming a specific run}

\item{methods}{Character vector of methods to use: any combination of
"hommola", "paco", "parafit" (default uses these three, all distance-based),
and the opt-in "topology" (a branching-order congruence test that ignores
branch lengths; each host is reduced to its largest single-host clade and
hosts with no coherent position are excluded, reported via the
Topology_hosts_used and Topology_fraction_kept columns)}

\item{focus_hosts}{Optional character vector of host names. For each name a
logical \verb{<host>_PRESENT} column is added to the results, TRUE at nodes whose
subtree contains that host. Useful for sorting/filtering the scan when you
care about co-diversifying symbionts in particular hosts. Names not found in
\code{Host_to_Symbiont_df} trigger a warning and are FALSE everywhere.}

\item{subtree_features}{If TRUE (default), calculates multiple tree distance
metrics (TreeDistance, SharedPhylogeneticInfo, etc.) for each node}

\item{cores}{Number of cores for parallelizing the node scan. If NULL
(default), uses all detected cores minus one. Forking is unavailable on
Windows, where the scan always runs on a single core.}

\item{batch_size}{Number of nodes to scan per checkpoint batch. Results are
written to \code{Save_fp} after each batch, so smaller values checkpoint more
often (more restart safety, slightly more disk I/O). If NULL (default), a
size of \code{max(200, cores * 50)} is used. Mutually exclusive with \code{n_batches}.}

\item{n_batches}{Alternative to \code{batch_size}: split the scanned nodes into
this many roughly equal checkpoint batches. Mutually exclusive with
\code{batch_size}.}

\item{continue}{If TRUE (default), resumes an interrupted scan by skipping
completed nodes; requires Save_fp file to exist}
}
\value{
A \code{codiv} object: a data frame (with one row per symbiont tree node
scanned) carrying the run parameters as a \code{codiv_params} attribute, with
\code{print} and \code{summary} methods. It behaves like a data frame for subsetting
and column access. Columns include:
\itemize{
\item Node_ID: Node identifier
\item N_Symbionts, N_Hosts: Tip counts in each subtree
\item Symbiont_Colless, Symbiont_Sackin: Tree shape metrics
\item Hommola_r, Hommola_pvalue: Hommola Pearson correlation on the collapsed
subtree and its permutation p-value (if the hommola method is used)
\item Uncollapsed_Hommola_r, Uncollapsed_Hommola_pvalue: the same test on the
uncollapsed subtree (only when \code{uncollapsed_hommola = TRUE})
\item PACo_ss, PACo_pvalue: PACo results (if method used)
\item ParaFitGlobal, ParaFit_pvalue: ParaFit results (if method used)
\item Topology_congruence, Topology_pvalue, Topology_hosts_used,
Topology_fraction_kept: topology-congruence results (if method used)
\item Tree distance metrics if subtree_features=TRUE
}

P-values are the per-node permutation p-values. They are not corrected for
multiple testing across nodes: nested clades are non-independent, so a
per-node correction (e.g. Benjamini-Hochberg) would be invalid. Use
\code{\link[=codiv_null_scans]{codiv_null_scans()}} for a scan-level false-discovery assessment.
}
\description{
Scans a symbiont phylogenetic tree for evidence of codiversification with
a host tree. For each internal node of the symbiont tree meeting size
thresholds, calculates correlation coefficients and p-values using one or
more methods (Hommola, PACo, ParaFit) to test if the symbiont topology
matches the host topology at that node.
}
\examples{
\donttest{
# simulate a small host/symbiont dataset with known co-diversification
sim <- simulate_codiv_data(n_hosts = 10, n_clades = 3, seed = 1)

results <- codiv(sim$host_tree, sim$symbiont_tree, sim$links,
                 methods = "hommola", permutations = 99, verbose = FALSE)
results            # compact overview
head(as.data.frame(results))

# Bundled example trees (real data) load with system.file():
# host <- ape::read.tree(system.file("extdata", "Host_Tree.nwk",
#                                    package = "codiv"))
}

}
