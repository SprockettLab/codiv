% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codiv_null_scans.R
\name{codiv_null_scans}
\alias{codiv_null_scans}
\title{Second-order permutation test for scan-wide co-diversification}
\usage{
codiv_null_scans(
  Host_tree,
  Symbiont_tree,
  Host_to_Symbiont_df,
  n_permutations = 100,
  statistic = NULL,
  stat_threshold = 0.75,
  pvalue = NULL,
  p_threshold = 0.01,
  observed = NULL,
  seed = 8675309,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{Host_tree}{A phylo object; binary host tree}

\item{Symbiont_tree}{A phylo object; binary symbiont tree}

\item{Host_to_Symbiont_df}{A data frame with "Host" and "Symbiont" columns}

\item{n_permutations}{Number of host-label permutations (outer null); the
analyses in the source papers used 100}

\item{statistic}{Name of the test-statistic column used to call a node
significant. NULL (default) uses \code{Hommola_r}, the collapsed Hommola
correlation that \code{\link[=codiv]{codiv()}} always writes when the Hommola method runs}

\item{stat_threshold}{A node counts as significant when its statistic exceeds
this value; default 0.75}

\item{pvalue}{Name of the p-value column used to call a node significant;
NULL (default) resolves alongside \code{statistic}}

\item{p_threshold}{A node counts as significant when its p-value is below this
value; default 0.01}

\item{observed}{Optional precomputed \code{\link[=codiv]{codiv()}} result for the real (unpermuted)
data; if NULL it is computed}

\item{seed}{Random seed for reproducibility}

\item{verbose}{If TRUE (default), report progress across permutations}

\item{...}{Additional arguments passed to \code{\link[=codiv]{codiv()}} (e.g. min_hosts,
min_symbiont_tips, span_fraction, permutations, methods, cores)}
}
\value{
A list with:
\itemize{
\item \code{observed}: number of significant clades in the real data
\item \code{null_counts}: number of significant clades in each permuted scan
\item \code{global_pvalue}: (permutations with count >= observed + 1) / (n + 1)
\item \code{empirical_fdr}: expected false-discovery proportion at the chosen
thresholds, \code{mean(null_counts) / observed} (capped at 1)
\item \code{thresholds}: the statistic/p-value columns and cutoffs used
}
}
\description{
Assesses whether a co-diversification scan detects more significant clades
than expected by chance, accounting for the phylogenetic non-independence and
pseudoreplication of nodes that per-node FDR corrections (e.g. Benjamini-
Hochberg) ignore. The host tree's tip labels are permuted and the entire
\code{\link[=codiv]{codiv()}} scan is rerun; repeating this builds a null distribution for the
\emph{number} of significant clades. Because each permutation reruns the whole
pipeline, the null captures exactly the dependence structure that a per-node
correction does not.
}
\details{
This is the recommended way to gauge false discoveries for a whole scan
(Sanders et al. 2023; Sprockett et al. 2025). \code{\link[=codiv]{codiv()}} deliberately does not
apply a per-node multiple-testing correction, which would be invalid for
non-independent nested clades.

Runtime is roughly \verb{(n_permutations + 1) x} a single scan, so use \code{cores} and
consider fewer inner \code{permutations} for a first pass. Each rerun uses its own
checkpoint file and does not resume.
}
\examples{
\donttest{
sim <- simulate_codiv_data(n_hosts = 8, n_clades = 2, seed = 1)
null <- codiv_null_scans(sim$host_tree, sim$symbiont_tree, sim$links,
                         n_permutations = 10, min_hosts = 3,
                         min_symbiont_tips = 7, span_fraction = 0.6,
                         permutations = 49, methods = "hommola",
                         verbose = FALSE)
null$observed
null$empirical_fdr
}

}
