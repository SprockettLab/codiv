% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_results.R
\name{filter_results}
\alias{filter_results}
\title{Filter a codiversification scan, optionally collapsing nested calls}
\usage{
filter_results(
  codiv_results,
  ...,
  hosts = NULL,
  hosts_match = c("all", "any"),
  nested = c("keep_all", "outermost", "innermost", "best"),
  statistic = "Hommola_r",
  sort_by_filters = FALSE
)
}
\arguments{
\item{codiv_results}{A \code{codiv} result (or data frame) from \code{\link[=codiv]{codiv()}}}

\item{...}{Filtering expressions evaluated inside \code{codiv_results}, in the
style of \code{\link[=subset]{subset()}}, for example \code{Hommola_pvalue < 0.05} and
\code{Hommola_r > 0.7}. Multiple expressions are combined with AND.
Rows where an expression is NA are dropped, matching \code{\link[=subset]{subset()}}.}

\item{hosts}{Optional character vector of host names to require. \code{\link[=codiv]{codiv()}}
writes a \verb{<host>_PRESENT} column for every name passed to its
\code{focus_hosts} argument, and those columns are what this reads, so a host
can only be filtered on if it was tracked during the scan.}

\item{hosts_match}{Whether a clade must contain all of \code{hosts} ("all", the
default) or at least one of them ("any")}

\item{nested}{How to treat nested calls, applied after every other filter:
\itemize{
\item \code{"keep_all"} (default) leaves them alone, so the result still counts one
lineage many times
\item \code{"outermost"} keeps the largest call in each nested series
\item \code{"innermost"} keeps the calls that contain no other surviving call.
A call can hold two disjoint subclades, so this can return more rows
than \code{"outermost"}, not fewer
\item \code{"best"} keeps whichever call in each series has the highest
\code{statistic}. The strongest signal often sits inside a series rather
than at its outermost call, so this frequently differs from
\code{"outermost"}
}}

\item{statistic}{Column used to choose a winner when \code{nested = "best"};
default "Hommola_r"}

\item{sort_by_filters}{If TRUE, order the surviving rows by the columns used
in \code{...}, in the order those conditions were written, taking the direction
from each comparison: \code{Hommola_pvalue < 0.01} sorts that column ascending,
\code{Hommola_r > 0.5} sorts it descending. Only simple \verb{column op constant}
comparisons give a direction, so compound conditions and \code{==} are ignored
for sorting. Default FALSE, which leaves the scan's own order alone.}
}
\value{
A \code{codiv} object holding the surviving rows, carrying the same
\code{codiv_params} attribute as the input plus a \code{codiv_filter} attribute
recording what was applied.
}
\description{
Subsets the output of \code{\link[=codiv]{codiv()}} on any combination of its columns, on which
hosts a clade contains, and on nesting. Scanned nodes are nested by
construction, so a single codiversifying lineage is usually reported many
times over: once for the whole clade and once for most of its subclades.
Counting those rows directly overstates how many distinct lineages were
found. The \code{nested} argument reduces each nested series to one call.
}
\details{
Filtering always runs before de-nesting. The other order would pick a
representative for a series and then possibly discard it, silently losing
the whole series even when other members passed the filter.

De-nesting reads the per-node symbiont trees stored by \code{\link[=codiv]{codiv()}}, so
\code{Symbiont_Tree} must still be present.
}
\examples{
\donttest{
sim <- simulate_codiv_data(n_hosts = 10, n_clades = 3, seed = 1)
res <- codiv(sim$host_tree, sim$symbiont_tree, sim$links,
             methods = "hommola", permutations = 99, verbose = FALSE)

# every significant call, including nested duplicates
filter_results(res, Hommola_pvalue < 0.05)

# one row per distinct lineage
filter_results(res, Hommola_pvalue < 0.05, nested = "outermost")
}

}
\seealso{
\code{\link[=codiv]{codiv()}}, \code{\link[=host_codiv_summary]{host_codiv_summary()}}
}
