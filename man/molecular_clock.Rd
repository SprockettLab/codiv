% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/molecular_clock.R
\name{molecular_clock}
\alias{molecular_clock}
\title{Molecular-clock corroboration of co-diversification}
\usage{
molecular_clock(
  codiv_results,
  host_time_tree,
  statistic = NULL,
  stat_threshold = 0.95,
  pvalue = NULL,
  p_threshold = 0.01,
  symbiont_divergence = NULL
)
}
\arguments{
\item{codiv_results}{A \code{codiv} result (or data frame) from \code{\link[=codiv]{codiv()}}}

\item{host_time_tree}{A time-calibrated host tree (phylo, ultrametric) whose
branch lengths are in time units (e.g. millions of years)}

\item{statistic}{Name of the test-statistic column. NULL (default) uses
\code{Hommola_r}, the collapsed Hommola correlation that \code{\link[=codiv]{codiv()}} always
writes when the Hommola method runs}

\item{stat_threshold}{Only clades with a statistic above this value are used;
default 0.95 (strong co-diversification)}

\item{pvalue}{Name of the p-value column. NULL (default) resolves
alongside \code{statistic}}

\item{p_threshold}{Only clades with a p-value below this value are used;
default 0.01}

\item{symbiont_divergence}{Optional override for the symbiont-divergence
summary. Either a named numeric vector keyed by \code{Node_ID}, or a function
taking a symbiont subtree (phylo) and returning a single number. If NULL
(default), the clade's crown depth is used (mean root-to-tip distance in
the symbiont tree's branch-length units)}
}
\value{
A list with:
\itemize{
\item \code{model}: the fitted \code{lm} of symbiont divergence on host age
\item \code{slope}, \code{intercept}, \code{r_squared}, \code{p_value}: regression summaries
\item \code{slope_ci}: 95\% confidence interval for the slope
\item \code{per_clade}: data frame with one row per clade used (\code{Node_ID},
\code{n_hosts}, \code{host_age}, \code{symbiont_divergence})
}
}
\description{
For the most strongly co-diversifying clades, regresses symbiont divergence
against host clade age. A positive relationship corroborates that symbiont
clades diversified contemporaneously with their hosts, and the slope is an
estimate of the symbiont molecular rate (substitutions per unit host time).
This follows the approach of Sanders et al. (2023) and Sprockett et al.
(2025).
}
\details{
Host clade age is the crown age of the clade's hosts (the age of their most
recent common ancestor in \code{host_time_tree}). The default symbiont divergence
is a simplification: it is the clade crown depth in substitution units, not a
dedicated absolute date. For rigorous absolute dating supply your own
estimates via \code{symbiont_divergence}. Nested clades are non-independent; the
regression treats each passing node as a point, so consider restricting to
non-nested clades for formal inference.
}
\examples{
\donttest{
sim <- simulate_codiv_data(n_hosts = 14, clades = list(
  list(codiversifying = TRUE, congruence = 1, n_per_host = 3)), seed = 1)
res <- codiv(sim$host_tree, sim$symbiont_tree, sim$links, span_fraction = 0.9,
             methods = "hommola", permutations = 199, verbose = FALSE)
# sim$host_tree is ultrametric, so it stands in for a time-calibrated tree
mc <- molecular_clock(res, sim$host_tree, stat_threshold = 0.75)
mc$slope
mc$per_clade
}

}
