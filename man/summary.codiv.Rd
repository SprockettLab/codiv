% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codiv_class.R
\name{summary.codiv}
\alias{summary.codiv}
\title{Summarize a codiv result}
\usage{
\method{summary}{codiv}(object, alpha = 0.05, ...)
}
\arguments{
\item{object}{A \code{codiv} object from \code{\link[=codiv]{codiv()}}.}

\item{alpha}{Significance threshold; default 0.05.}

\item{...}{Ignored.}
}
\value{
A data frame with one row per method (invisibly returned by print),
containing the statistic range and the per-node significant count.
}
\description{
Per-method summary of a codiversification scan: the range of the primary
statistic and the number of nodes with a per-node permutation p-value below
\code{alpha}. This p-value count is descriptive and not corrected across nodes;
use \code{\link[=codiv_null_scans]{codiv_null_scans()}} for a scan-level false-discovery assessment.
}
\examples{
\donttest{
sim <- simulate_codiv_data(n_hosts = 10, n_clades = 3, seed = 1)
res <- codiv(sim$host_tree, sim$symbiont_tree, sim$links,
             methods = c("hommola", "paco"), permutations = 99)
summary(res)
summary(res, alpha = 0.01)   # stricter significance threshold
}

}
